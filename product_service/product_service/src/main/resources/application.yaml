spring :
  datasource:
    url: jdbc:mysql://localhost:3306/micro_products
    username: root
    password: kien1610

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8888